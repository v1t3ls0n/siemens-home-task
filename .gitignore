# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Runtime state (caches, local vector store, analysis history)
*.db
localstore.json
.chroma/

# OpenSearch / docker volumes (if ever mounted locally)
osdata/

# Batch-eval output (regenerable). To showcase a run:
#   git add -f eval_results/summary.md
eval_results/

# IDE / OS noise
.vscode/
.idea/
.DS_Store
